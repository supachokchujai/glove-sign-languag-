#ifndef GLOVE_CONFIG_H
#define GLOVE_CONFIG_H

// ============================================================
//  ไฟล์ตั้งค่าเดียวของถุงมือ (Firmware)
//  แก้ไขค่าตรงนี้ที่เดียว — ไม่ต้องแก้ใน main.ino
//  ฝั่ง Python มี config.py ให้แก้ให้ตรงกัน
// ============================================================

// --- WiFi ---
// เปลี่ยน SSID / รหัสผ่านให้ตรงกับเครือข่ายที่ใช้
const char* WIFI_SSID     = "";
const char* WIFI_PASSWORD = "";

// --- UDP ปลายทาง (คอมพิวเตอร์ที่รัน Python) ---
// เปลี่ยนเป็น IP ของคอมพิวเตอร์ที่รัน savedata1.py / monitor_*.py / web.py
// (หาได้จาก `ipconfig` บน Windows หรือ `ip addr` บน Linux/Mac)
const char* UDP_TARGET_IP   = "172.20.10.13";
const int   UDP_TARGET_PORT = 4210;   // ต้องตรงกับ UDP_PORT ใน config.py

// --- ความถี่การส่งข้อมูล ---
// 50 ms = ~20 Hz  -> 60 เฟรม/ท่า ใช้เวลาประมาณ 3 วินาที
// 30 ms = ~33 Hz  -> 60 เฟรม/ท่า ใช้เวลาประมาณ 1.8 วินาที
// หมายเหตุ: ถ้าเปลี่ยนค่า ต้องเก็บข้อมูลและฝึกโมเดลใหม่
//           เพราะโมเดลเรียนรู้จากอัตราเฟรมเดิม (20 Hz)
const int SEND_DELAY_MS = 50;

// --- ขา Flex Sensor (ADC) ---
// ลำดับ: นิ้วโป้ง, นิ้วชี้, นิ้วกลาง, นิ้วนาง, นิ้วก้อย
const int FLEX_PINS[5] = {35, 34, 39, 36, 32};

#endif  // GLOVE_CONFIG_H
